# find_package(Qt6 CONFIG REQUIRED COMPONENTS network)
# find_package(Qt6 CONFIG REQUIRED COMPONENTS core)

# add_library(http STATIC rest_http_handler.cpp)

# target_link_libraries(http Qt6::Network)
# target_link_libraries(http Qt6::Core)

# target_include_directories(http PUBLIC "../include")